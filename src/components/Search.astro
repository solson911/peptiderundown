---
// Search.astro — renders ONE modal globally + a trigger button
// Drop <Search /> once in Base.astro; the modal is shared.
---

<!-- Trigger button (works for both desktop and mobile) -->
<button id="search-trigger" aria-label="Search" class="flex items-center gap-1.5 px-3 py-1.5 text-sm font-medium text-text-muted hover:text-primary transition-colors rounded-lg hover:bg-gray-100">
  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
  </svg>
  <span class="hidden sm:inline text-sm">Search</span>
</button>
